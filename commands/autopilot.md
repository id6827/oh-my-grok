---
description: Full autonomous pipeline (alias → skill autopilot)
---

Follow the oh-my-grok skill `/autopilot` with the user's arguments.
Use `.omg/specs/` and `.omg/plans/` when present. State under `.omg/state/`.
