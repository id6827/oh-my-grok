# Dependencies / build
node_modules/
# Keep dist/runtime committed optional — prefer npm run build in CI
# dist/ ignored only if regenerable; we commit dist/runtime for plugin consumers
*.log
.DS_Store

# OMG runtime state (project-local)
.omg/**
!.omg/skills/
!.omg/skills/**

# Editor / local
.idea/
.vscode/
*.swp
.env
.env.*
