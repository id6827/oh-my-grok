# Benchmarks

OMC ships full prompt benchmarks under `benchmarks/`. OMG keeps a minimal placeholder.

## Status

🟡 Runner not fully ported. See `docs/OMC-PORT-STATUS.md`.

To port: copy OMC `benchmarks/` and adapt model provider calls to Grok.

```bash
# Future
# npm run bench:prompts
```
