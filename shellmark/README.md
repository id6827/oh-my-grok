# shellmark

Session capture format used by some OMC/OMG tooling demos.

**Do not commit** live capture trees under `sessions/` — they are gitignored runtime dumps.
If you need a fixture for tests, add a minimal synthetic tree under `tests/` instead.
